// src/utils/axiosInstance.ts

// NOTE: This is a small wrapper around the shared API client.
// It exists for backwards-compatibility with older imports.
import { apiRoot } from "@/lib/api";

export default apiRoot;
